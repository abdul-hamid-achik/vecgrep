# Studio (removed)

The interactive Studio terminal UI is not shipped in the CLI for now.

Use:

- [Quick Start](/quick-start) for human CLI workflows
- [CLI Usage](/usage) for the full command reference
- [MCP Integration](/mcp) for assistant integration

All operations are available through the CLI (`--json` for machines) and the MCP server (`vecgrep serve`).
